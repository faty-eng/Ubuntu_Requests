# Ubuntu Image Fetcher

> "I am because we are." — Ubuntu Philosophy

A Python script for respectfully downloading and organizing images from the web.

## Features
- Handles multiple URLs
- Prevents duplicate downloads
- Checks file types before saving
- Saves all images in `Fetched_Images/`

## Run
```bash
python ubuntu_image_fetcher.py
